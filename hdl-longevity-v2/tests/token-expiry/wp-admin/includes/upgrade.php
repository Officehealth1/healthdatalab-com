<?php
// Test stub — ABSPATH points at tests/token-expiry/, so the activator's
// `require_once ABSPATH . 'wp-admin/includes/upgrade.php'` lands here.
function dbDelta( $queries = '', $execute = true ) { return array(); }
