{
    "name": "healthdatalab-com",
    "version": "1.0.0",
    "description": "HealthDataLab website with Stripe integration",
    "main": "index.html",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "nodemailer": "^8.0.1",
        "stripe": "^14.25.0"
    }
}
